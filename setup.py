from setuptools import setup, find_packages

setup(
    name='postcode-validator-',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    description='Tool for validation postcodes',
    author='dubrovskyi',
    license='MIT',
)
